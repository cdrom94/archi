export interface ArchitecureData {
	name: string;
	location: string;
	cursor: string;
	built: number;
	architect: string;
	area: string;
	quote: string;
	link: string;
	carousel: string[];
}

export interface ArchitecureTransition {
	duration: number;
	delay: number;
	ease: number[];
}
